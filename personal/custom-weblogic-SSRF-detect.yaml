id: custom-weblogic-SSRF-detect

info:
        name: Custom-logapi-log-detect
        author: 0xunknown
        severity: low
        tags: panel

requests:
        - method: GET
          path:
                  - "{{BaseURL}}/uddiexplorer/SearchPublicRegistries.jsp"
          matchers:
                  - type: word
                    words:
                            - "<title>Search public registries</title>"
